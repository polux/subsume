*Disclaimer: this is not an official Google product*

Attempts at minimizing a set of patterns (as in pattern matching).

How to compile the web version:

 * install nix
 * run `nix-env -iA cachix -f https://cachix.org/api/v1/install`
 * run `cachix use miso-haskell`
 * in the repo, run `nix-build`
 * serve the content of out using `python -m SimpleHTTPServer 8000` or other since chrome doesn't like symlinks
